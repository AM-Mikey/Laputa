[gd_resource type="Shader" format=3 uid="uid://darqkbw2t33ba"]

[resource]
code = "shader_type canvas_item;

void fragment(){
    vec2 newuv = UV;
    newuv.x += TIME;
	newuv.y -= TIME;
    vec4 c = texture(TEXTURE, newuv);
    COLOR = c;
}"
